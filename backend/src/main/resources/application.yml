spring:
  application:
    name: hospital-medicine-management
  
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:hospital_medicine}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:hospital}
    password: ${DB_PASSWORD:hospital123}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: false
    out-of-order: true
  
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:96213116248-r1aabopumjdbrdg40tvjm77d109a8f1m.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            scope:
              - email
              - profile

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:ab0FCi62o86vBrQv1Q4cXl0UV+Ev3Viww5BOnmJX3xM=}
  expiration: 86400000 # 24 hours
  refresh-expiration: 604800000 # 7 days

# Google OAuth
google:
  client-id: ${GOOGLE_CLIENT_ID:96213116248-r1aabopumjdbrdg40tvjm77d109a8f1m.apps.googleusercontent.com}

# Server Configuration
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

# CORS Configuration
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Logging
logging:
  level:
    com.hospital: DEBUG
    org.springframework.security: DEBUG
